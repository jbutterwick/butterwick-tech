{
  "scripts": {
    "build:resume-css": "tailwindcss -i ./static/input.css -o ./static/output.css",
    "build:site": "npm run build:resume-css && zola build && python3 scripts/normalize_html.py public",
    "check:site": "npm run build:site && python3 scripts/verify_site.py public",
    "check:resume": "npm run build:site && python3 scripts/verify_resume.py public",
    "check": "npm run build:site && python3 scripts/verify_site.py public && python3 scripts/verify_resume.py public",
    "serve": "zola serve"
  },
  "dependencies": {
    "@tailwindcss/cli": "^4.1.17",
    "tailwindcss": "^4.1.17"
  }
}
